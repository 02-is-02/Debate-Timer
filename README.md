# Tauri + React + Typescript

This is a debate timer project built with Tauri + React + Typescript

Install & Run (Preview version)
1. Clone repo
2. Install dependancies
   ```bash
   npm install
   ```
3. Run local server
   ```bash
   npm run tauri dev
   ```
